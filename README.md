# Mux-Sub
A universal FFmpeg-based muxer for anime and donghua. Automatically matches episodes and subtitles, embeds fonts, adds optional chapters, preserves audio tracks, and supports custom output naming.
